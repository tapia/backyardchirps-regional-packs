# Everything downloaded or generated: the models, the eBird rasters and GeoPackages, the
# refreshed taxonomy. Gigabytes, and all of it re-fetchable.
work/

# Built packs.
dist/

# The two API keys, kept where `source env` can find them. Only this repository ever holds them:
# a station holds neither.
env

.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.claude/
.DS_Store
.idea